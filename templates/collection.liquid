{% comment %} Template de coleção {% endcomment %}
<h1>{{ collection.title }}</h1>
<ul>
{% for product in collection.products %}
  <li>
    <a href="{{ product.url }}">{{ product.title }}</a>
  </li>
{% endfor %}
</ul>
